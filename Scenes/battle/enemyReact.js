function enemyReact() {
    // if (level == 'Easy') {
    //     return [Math.floor(Math.random() * 3)][0.7, 0.8, 0.9, 1]
    // }
    // else if (level == 'Normal') {
    //     return [Math.floor(Math.random() * 2)][0.4, 0.5, 0.6]
    // }
    // else if (level == 'Hard') {
    //     return [Math.floor(Math.random() * 2)][0.1, 0.2, 0.3]
    // }

    return 1
}